{
  "name": "musicfree",
  "private": true,
  "version": "1.0.0",
  "description": "MusicFree - Cloudflare Workers + Vue3",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
